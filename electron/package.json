{
  "name": "pgAdmin",
  "version": "4.10.0",
  "description": "pgAdmin is the most popular and feature rich Open Source administration and development platform for PostgreSQL, the most advanced Open Source database in the world.",
  "main": "src/index.js",
  "repository": "git.postgresql.org",
  "author": "pgAdmin Hackers",
  "license": "PostgreSQL",
  "scripts": {
    "start": "yarn run electron .",
    "start:dev": "cross-env NODE_ENV=dev yarn run electron .",
    "package": "",
    "make": "",
    "publish": "",
    "dist:deb": "",
    "dist:rpm": "",
    "dist:darwin": "electron-builder --mac --x64",
    "dist:windows": "electron-builder --win --x64",
    "linter": "yarn eslint --no-eslintrc -c .eslintrc.js --ext .js .",
    "copy-folder:nix": "cp -r ../web web && find web | grep .pyc$ | xargs -I '{}' rm '{}'",
    "build:nix": "yarn copy-folder:nix && yarn bundle-app-js",
    "test": "yarn lint && cross-env ENV=TEST yarn mocha"
  },
  "keywords": [
    "pgadmin",
    "pgadmin4",
    "postgres",
    "sqleditor",
    "sql",
    "query",
    "database"
  ],
  "build": {
    "appId": "org.postgresql.pgadmin",
    "productName": "pgAdmin",
    "asar": false,
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": [
        "zip"
      ],
      "icon": "assets/icons/pgAdmin4.icns"
    },
    "nsis": {
      "perMachine": true
    },
    "publish": [
      {
        "provider": "generic",
        "url": "http://0.0.0.0:8000/"
      }
    ]
  },
  "dependencies": {
    "axios": "^0.18.0",
    "bootstrap": "^4.3.1",
    "cross-env": "^5.1.5",
    "electron-squirrel-startup": "^1.0.0",
    "electron-updater": "^4.1.2",
    "jquery": "^3.3.1",
    "logform": "^1.6.0",
    "popper.js": "^1.15.0",
    "winston": "^3.0.0-rc5"
  },
  "devDependencies": {
    "electron": "^6.0.7",
    "electron-builder": "^21.2.0",
    "eslint": "^6.3.0"
  }
}
